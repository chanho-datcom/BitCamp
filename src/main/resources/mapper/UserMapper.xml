<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.reactboard.mapper.UserMapper">
    <select id="auth" resultType="user" parameterType="user">
        select * from user where username = #{username} and password = #{password}
    </select>
    <insert id="insert" parameterType="user" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password, nickname, grade)
        values (#{username}, #{password}, #{nickname}, #{grade});
    </insert>
    <select id="selectOne" resultType="user" parameterType="int">
        select * from user where id=#{id}
    </select>
    <update id="update" parameterType="user" useGeneratedKeys="true" keyProperty="id">
        update user set nickname=#{nickname}, password=#{password}, grade=#{grade} where id=#{id}
    </update>
    <delete id="delete" parameterType="int">
        delete from user where id=#{id}
    </delete>
</mapper>